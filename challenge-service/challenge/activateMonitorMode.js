// Handle the case if some dispute was opened by bad actor, for example the same entity that
// has made claim with incorrect result and wants to push it by disputing with itself and losing
// as a prosecutor.

function activateMonitorMode(commitmentRoot) {
  return;
}

module.exports = activateMonitorMode;
